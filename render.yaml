services:
  - type: web
    name: logo-bot
    runtime: python
    pythonVersion: 3.11
    buildCommand: python -m pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: BOT_TOKEN
        sync: false
